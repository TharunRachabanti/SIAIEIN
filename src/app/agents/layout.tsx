import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI Agents | SIAIEIN Agentic AI",
  description: "Explore our specialized AI agents for Customer Support, Sales Development, and Business Operations.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
